# 控制页整体 UI 升级报告（REVIEW9）

> 目标：手机扫码打开的控制页——整体布局与配色深度优化
> 框架：ui-ux-pro-max（布局层级/配色/触控/一致性）

---

## 一、布局重构（卡片化分区）

之前：平铺分区（状态栏、地址栏、快捷区、文字区、遥控区直接堆叠）。
现在：**语义卡片分组 + 分区标题**——

| 分区 | 内容 |
|---|---|
| 品牌头部 | 玫红渐变圆角品牌标（电视图标）+ "电视遥控 / TV Browser Remote" + 右侧连接状态（信息右对齐 + 发光状态点） |
| 访问网址 | 输入框 + 打开/搜索（渐变主按钮） |
| 快捷入口 | 3 列站点网格 + 编辑入口 |
| 编辑快捷入口 | 增删改面板（隐藏时不可见） |
| 文字输入 | 输入框 + 发送 |
| 遥控器 | 圆形 D-pad + 侧键 2×2 |

要点：分区标题（11px 字距 0.1em）、卡片表面（渐变 border-gradient、16px 圆角）、8px 间距节奏、信息右对齐的头部布局。

## 二、配色升级（精致深色主题）

- 背景：近黑 #0b0b10 + 顶部玫红径向微光 + 角落绿微光（层次感，非纯黑平板）
- 主色：玫红线性渐变（#ff3d7f → #ff7aa8），应用于品牌标、打开/搜索/OK/保存按钮 + 柔和投影
- 成功绿渐变（保存/发送）；危险红柔化（删除按钮 18% 透明底）
- 文本三层级：主 #f2f2f5 / 次 #9a9aa8 / 弱 #666674
- 边框 rgba 白 8%（微光分割）

## 三、遥控器视觉

- **圆形 D-pad**：上/下/左/右圆形按键（82px）+ 中心 OK（渐变、发光投影）——更接近真实遥控器
- 侧键 2×2 竖排（返回/菜单/主页/刷新），横排图标+文字
- 按压反馈：scale(.92-.95) 微缩 + 主色填充（触感替代提示）

## 四、细节与可访问性

- 输入框 focus 边框高亮（主色/成功色）
- 图标按钮 aria-label 保留；.key focus-visible 焦点环保留
- 全 icon 库保留（15 个 SVG symbol）
- 过渡动画 120-150ms（按压微缩、状态点变色）

## 五、兼容性

- app.js 依赖 id/class 全部保留（14 个 id + data-key + .site 动态创建）
- flashOk 兼容带 span 按钮（OK/侧键/地址/发送均含 span）
- 移动端安全区（env(safe-area-inset-top/bottom)）已适配

## 六、验证

- JS 语法：app.js / index.html 通过
- assembleDebug/Release：BUILD SUCCESSFUL 零警告
- verify.js：29/29
- release 已重打（09:36），包内含品牌头部/圆形 pad/渐变按钮，签名有效
